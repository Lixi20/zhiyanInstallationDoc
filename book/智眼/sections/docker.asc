启动docker：
sudo systemctl start docker

构建镜像：
 docker build --tag java-docker .

列出本地镜像：
docker image ls

删除python语言的docker标签：rmi命令
docker rmi python-docker:v1.0.0

删除为运行的容器：
docker rm + 容器ID

列出运行docker容器：
docker ps

列出所有的docker容器（不管状态）：
docker ps -a

将镜像以容器运行: 
docker run 镜像ID或者名字

停止容器：
docker stop 容器名称或者ID

查看机器上所有的容器（无论是启动还是停止状态）：
docker ps -a

连接docker：
docker exec -it 容器名称 bash 或者 ssh 容器IP

指定端口：
 docker run --publish 8000:5000 python-docker
*--publish命令，将主机端口8000映射到端口5000

重启容器：
 docker restart 容器名称或者ID

解决随机命名问题：
 docker run -d -p 8000:5000 --name rest-server python-docker
*--name命令，rest-server是新命名，python-docker是容器名字


查看容器IP：
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' + 容器ID


您可以使用适用于您的操作系统的任何安装程序或使用 docker 在本地启动 PostgreSQL：
docker run -e POSTGRES_USER=petclinic -e POSTGRES_PASSWORD=petclinic -e POSTGRES_DB=petclinic -p 5432:5432 postgres:14.1

清理docker缓存：
docker system prune : 可以用于清理磁盘，删除关闭的容器、无用的数据卷和网络，以及 dangling 镜像（即无 tag 的镜像）。
docker system prune -a : 清理得更加彻底，可以将没有容器使用 Docker镜像都删掉。
注意，这两个命令会把你暂时关闭的容器，以及暂时没有用到的 Docker 镜像都删掉了。

挂在本地文件：
docker run -t --name zhiyan-server -p 9091:9091 -v /home/lixi/workspace/ZhiYan/java-zhiyan-server/src/main/resources/logback.xml:/home/lixi/workspace/ZhiYan/java-zhiyan-server/src/main/resources/logback.xml zhiyan-server

docker内查看进程：
ps -ef



上传dockerhub:
#语法说明 docker tag local-image:tagname new-repo:tagname
#语法说明 docker push new-repo:tagname

docker tag docker-centos6-zhiyan:latest lloishii/docker-centos6-zhiyan:latest

docker push lloishii/docker-centos6-zhiyan:latest